# Бриф: Premium-сторінка, продовження Т-B: таблиця порівняння (premiumAccess.model у messages.bundle.js) і блок Access model досі дивінаційні — привести до реального пейволла в EN і uk

Поставлено: 2026-08-02 (Олександр, з панелі)
Виконавець: dev

## Контекст
Знахідка Олександра 2026-08-02 після заливки білда 58. Два місця: (1) compare-таблиця premiumAccess.model.* — рядки readings/interpretation/horoscope/compatibility/history, жодного астро-рядка; додати рядки про збережені місця спостереження (free: 1 місце? — звір з кодом skyFavoritesCore), інсайти щоденника (free: настрій сьогодні / premium: 30-денний мікс і серії), пакет супутників (free: МКС / premium: +Тяньгун, Габбл), історію; дивінаційні рядки лишити, але ПІСЛЯ астрономічних. (2) accessModel.free/premium — переписати: free веде з неба і щоденника (вони безкоштовні повністю — аргумент 4.3(b)), premium веде з savedPlaces/journalInsights/satellites, unlimited readings останнім. ОБИДВІ локалі messages.bundle.js (en і uk), рендер через ключі. Порядок як у getPremiumDetailItems. Після правки: lint, тести, перегенерувати шот 8 генератором tests/visual/appstore-shots.spec.js. Нових фіч нуль — тільки тексти і склад рядків таблиці.

## Питання
Premium-сторінка, продовження Т-B: таблиця порівняння (premiumAccess.model у messages.bundle.js) і блок Access model досі дивінаційні — привести до реального пейволла в EN і uk

## Критерії вердикту
(сформульовано dev 2026-08-02 перед роботою)
1. Кожен рядок таблиці і кожне твердження в `accessModel` звірені з живим гейтингом у коді;
   де код і бриф розходяться — виграє код, розбіжність названа у вердикті.
2. Астрономічні рядки першими, дивінаційні після них; порядок за `getPremiumDetailItems()`,
   рядок про безліміт таро — останній.
3. Обидві локалі (`en`, `uk`) в `src/i18n/messages.bundle.js`, рендер через ключі.
4. Нових фіч і змін гейтингу нуль — тільки тексти і склад рядків.
5. Регресія: lint exit 0, тести на тій самій базі (370), шот 8 перегенеровано штатним
   генератором, шоти 1–7 не змінені.

## Куди вердикт
Файл у artifacts/reports/; сюди — розділ «## Вердикт 2026-MM-DD (dev)»;
рядок у STATUS «Далі» оновити за конвенцією ядра.

## Вердикт 2026-08-02 (dev)
Зроблено: `artifacts/reports/2026-08-premium-compare-table.md`.
Таблиця — 5 дивінаційних рядків → **8**, астро першими (місця спостереження 1/без ліміту;
інсайти щоденника 7 днів/30 днів + серії; супутники МКС/МКС+Тяньгун і Габбл), далі теми,
розбір, сумісність, історія читань, а **розклад таро останнім**. Лід і підсумок таблиці та
блок `accessModel` (free/premium) переписані: free веде з неба і щоденника, premium — із
savedPlaces/journalInsights/satellites, безліміт таро останнім. Обидві локалі, лише ключі.
Числа звірені з кодом: `FREE_FAVORITE_LIMIT = 1`, `JOURNAL_PATTERNS_WINDOW_DAYS = 7` (free,
без гейта) проти `JOURNAL_INSIGHTS_WINDOW_DAYS = 30` (premium), `PREMIUM_SATELLITES = [css, hst]`.
Розбіжність із брифом: free-інсайти — це **зведення за 7 днів**, а не «настрій сьогодні».
Регресія: lint exit 0, **370/370** (та сама база), рендер обох локалей перевірено на живій
сторінці, шот 8 перегенеровано штатним генератором в обох розмірах, шоти 1–7 байт-у-байт ті самі.
Поза обсягом (не чіпав): `inbox/2026-08-02-premium-grid-tarot-tile-fourth.md` — плитка
«Unlimited Tarot» у сітці пейволла четверта, бо живий список — `featureItems`, а
`getPremiumDetailItems()` (який правив B3) ніде не рендериться.
