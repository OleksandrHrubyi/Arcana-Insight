name: CI Tests

on:
  push:
    branches:
      - main
      - master
      - develop
      - 'feature/**'
  pull_request:

jobs:
  lint_and_test:
    name: Lint and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm test

      # The runner sits in UTC, where a device-vs-location timezone bug is
      # invisible: the sky screens compute the observer's day, and anchoring it to
      # the machine's midnight only breaks off-zero offsets. Two extra runs — one
      # west of UTC, one east — catch that regression here instead of on a device.
      - name: Unit tests (device west of UTC)
        run: npm test
        env:
          TZ: America/Los_Angeles

      - name: Unit tests (device east of UTC)
        run: npm test
        env:
          TZ: Australia/Sydney

  edge_function_tests:
    name: Edge Function Tests (Deno)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Deno tests (money paths - premium gate, RC webhook, AI quota)
        # DENO_NO_PACKAGE_JSON: the repo-root package.json flips Deno into byonm
        # mode, and this job has no node_modules — type resolution then fails on
        # npm:@types/node. Ignoring package.json makes npm: imports resolve from
        # the Deno cache instead.
        run: deno test --allow-env --allow-net supabase/functions/tests/
        env:
          DENO_NO_PACKAGE_JSON: '1'
