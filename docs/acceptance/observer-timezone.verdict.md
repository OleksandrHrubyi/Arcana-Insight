# Вердикт QA · observer-timezone

> Порушення гарантії «сліпоти», чесно: (1) у контексті сесії автоматично був `git log` — 5 останніх
> коміт-меседжів, серед них `fix(sky): anchor dawn search to sunrise…`; (2) `STATUS.md` розділ «Далі»
> я відкрив ПІСЛЯ того, як усі перевірки були прогнані, щоб дописати свій рядок — на розбір не вплинуло.
> `docs/specs/observer-timezone.md` не читав.

Дата: 2026-08-01
Вхід: незакомічений дифф робочого дерева (12 змінених + `src/helpers/skyTime.js`, `tests/skyTimezone.test.js`)
і `docs/acceptance/observer-timezone.md`.

## Вердикт: НЕ ПРИЙНЯТО

Розрахункова частина (A1–A6, B3, C1–C3) виконана і перевірена незалежно — я не покладався на
авторські тести, а прогнав власний зріз по всіх 10 містах у трьох поясах пристрою, включно з
UTC+14, якого в критеріях немає. Не прийнято через 1 провалений критерій (D2) і 3 критерії
без доказу, який критерії вимагають прямо (B1, C4, D4).

## Розбір за критеріями

| # | Стан | Доказ |
|---|------|-------|
| A1 | ✅ | Власний прогін: 10 міст × поясів пристрою `Europe/Kyiv`, `America/Los_Angeles`, `Pacific/Kiritimati` — усюди `sunrise < sunset`, тривалість додатна. Плюс тест A1 (40 комбінацій) зелений у 5 поясах. |
| A2 | ✅ | Тест A2/A3 порівнює `JSON.stringify` (Date → ISO) для 4 поясів; мій прогін дає побітно ті самі рядки в 3 поясах — див. таблицю нижче. |
| A3 | ✅ | Усі шість функцій приймають `{ zone }`, обидві сторінки його передають (`SkyHomePage.vue:384-390`, `SkyPage.vue:707-715`). `localMidnight` у `skyCore.js:166` більше не читає годинник машини. `bodyViewTimes` (час планет/Місяця на SkyPage) якорить не на північ, а на `now` — від поясу не залежить взагалі. |
| A4 | ✅ | 2026-08-01, пристрій Europe/Kyiv: Сідней **10.46 год** (<12, зима пд. півкулі), Токіо **13.95 год**, ЛА **13.82 год** (>13), Київ **15.28 год** — збіг з базою ±1 хв. |
| A5 | ✅ | Мій прогін дав громадянські зсуви для всіх 10 міст (критерій вимагав 7): Київ UTC+3, Лондон UTC+1, Нью-Йорк UTC−4, ЛА UTC−7, Токіо UTC+9, Сідней UTC+10, Дубай UTC+4. `lon/15` не протік: тест A5 фіксує різницю 58 хв для Києва. |
| A6 | ✅ | `resolveObserverZone`: `source: 'gps'` → пояс пристрою (`skyTime.js:98`); тест A6 показує збіг заходу з вшитим Києвом. |
| B1 | ⚠️ **немає доказу, якого вимагає критерій** | Обчислення й форматування перевірені: на LA-пристрої дефолтний Київ дає **05:25 / 20:42 / 15год 17хв (−3 хв)** — київські години. Але критерій просить **скріншот симулятора/пристрою**; його немає, рендер-шлях перевірено читанням коду, не очима. |
| B2 | ✅ код / ⚠️ візуально | Ключ `skyHome.timeZoneNote` є і в `en` (2501), і в `uk` (5138) блоці `messages.bundle.js`; хардкоду немає, підстановка через `tt(key, vars)`. На головній — бейдж `UTC±N` біля міста + рядок у шторці Сонця; на SkyPage — рядок у блоці Сонця. Обидва під `showZoneNote`. Скріншота, знову ж, немає. |
| B3 | ✅ | `formatDayLength` віддає «—» для `-33840000`, `0`, `null`, `NaN` (тест B3), а `computeSunDetail` тепер сам віддає `null` замість інвертованої доби (тест R4, Тромсе). |
| C1 | ✅ | `TZ=America/New_York npm test` → **356/356**, 0 fail (було 342/345). |
| C2 | ✅ | 5 прогонів: `UTC`, `Australia/Sydney`, `America/Los_Angeles`, `Europe/Kyiv`, `America/New_York` — усі **356/356**. |
| C3 | ✅ | `git diff --stat -- src/helpers/dailyRitual.js` — **порожньо**. Полагоджено фікстури (`new Date(2026, 2, 9, 12, 0, 0)` замість UTC-літералів), продуктову логіку стріку не чіпали. |
| C4 | ⚠️ **немає доказу** | Кроки «Unit tests (device west/east of UTC)` з `TZ: America/Los_Angeles` / `Australia/Sydney` є в job «Lint and Test» (`ci-tests.yml:39-47`). Але критерій просить посилання на зелений прогін ПІСЛЯ мержу — код не закомічений, прогону не існує. |
| D1 | ✅ | `npm run lint` — без виводу, без помилок. |
| D2 | ❌ **провал** | `npm run ai:scan:all` → **3 issues** проти зрізу **2**. Новий warning: `suspect-generated-ios-config` на `ios/App/App/config 2.xml`. Файл до логіки поясів стосунку не має (є нотатка `inbox/2026-08-01-stray-ios-config-copy.md`), але він лежить у робочому дереві цього діффу і порушує file-hygiene з CLAUDE.md («ніколи не створюй файли з суфіксом ` 2`»). Критерій сформульований як число — число зросло. |
| D3 | ✅ | У шаблонах обох сторінок — тільки додавання: бейдж поясу (`SkyHomePage.vue:17`) і рядок-пояснення (`:225`, `SkyPage.vue:246`). Жоден блок не видалено, не переставлено, CTA не чіпали. Додані елементи — не декор, їх прямо вимагає B2. |
| D4 | ⚠️ **немає доказу** | По коду нових мережевих викликів немає: `skyTime.js` користується лише `Intl` (дані поясів з ОС), у діффі не з'явилось жодного `fetch`. Але критерій просить прогін у режимі «в літаку» / вкладку мережі — цього не робилось. |

### Незалежний зріз (не авторський тест), 2026-08-01, `computeSunDetail`

Три пояси пристрою — `Europe/Kyiv`, `America/Los_Angeles`, `Pacific/Kiritimati` (UTC+14, поза
переліком критеріїв) — дали **ідентичні** рядки:

```
kyiv       UTC+3   05:25 → 20:42   15.28h   15год 17хв (−3 хв)
warsaw     UTC+2   04:56 → 20:26   15.50h
london     UTC+1   05:24 → 20:48   15.41h
berlin     UTC+2   05:25 → 20:58   15.54h
paris      UTC+2   06:23 → 21:29   15.09h
newYork    UTC−4   05:52 → 20:11   14.31h
losAngeles UTC−7   06:04 → 19:53   13.82h
tokyo      UTC+9   04:49 → 18:45   13.95h
dubai      UTC+4   05:46 → 19:04   13.30h
sydney     UTC+10  06:47 → 17:15   10.46h
```

## Межі (перевірено окремо)

- **Перепризначення `TZ` всередині процесу** — тест A1/A2 стоїть на припущенні, що node
  підхопить `process.env.TZ` на льоту. Перевірив: node v22.23.1 підхоплює (LA 420 / Сідней −600).
  Тести не порожні.
- **Перехід на DST** — `zoneLocalMidnight` робить два проходи. Перевірив обидва напрямки для Києва:
  зимова північ `2026-01-14T22:00Z`, літня `2026-07-31T21:00Z` — правильно з обох боків переводу.
- **Невідомий id поясу** (сміття в Preferences) — `isValidTimeZone` відсіює, далі фолбек на довготу;
  `zoneLocalMidnight({timeZone:'Not/AZone'})` віддає валідну дату, не кидає.
- **Порожній вхід** — `resolveObserverZone(null)` → пояс пристрою.
- **Гонка «перший кадр»** — `loadSkyLocation()` чекають ДО першого `recompute()` на обох сторінках
  (`SkyHomePage.vue:605-608`, `SkyPage.vue:700`), а `watch(loc, …, {deep:true})` перераховує при зміні
  міста. Кадру зі старим поясом немає.
- **Подвійний виклик** — усі нові функції чисті, повтор дає той самий результат.
- **Мігрований інстал** — запис без `timeZone` добивається з `cityTimeZone(cityKey)`; для GPS-запису
  виходить `null` → пояс пристрою. Це збігається з A6.

## Поза межами задачі — що ще зачепив цей дифф

1. **Календар Місяця на SkyPage лишився на годиннику пристрою.** `viewYear/viewMonth`
   (`SkyPage.vue:341-342`) і мітка `isToday` (`:413-415`) будуються з `new Date()` пристрою, тоді як
   заголовок дати на тій самій сторінці тепер друкується за поясом локації. Пристрій LA + локація
   Київ, після 14:00 за LA: шапка скаже «2 серпня», а в календарі підсвічене 1 серпня. Критерії цього
   не покривають — але це нова видима неузгодженість всередині одного екрана, породжена саме цією
   правкою. Потрібне рішення: або календар теж на пояс локації, або свідомо лишити (це «календар
   користувача», не «календар місця») і записати в ADR.
2. **Застарілі GPS-координати після переїзду.** `source: 'gps'` рахується за поясом пристрою, а
   координати лишаються старими і самі не оновлюються (`detectSkyLocation` тільки по тапу). Користувач,
   що прилетів з Києва до LA, отримає київські координати на добі LA — і **без** мітки поясу, бо
   `zoneMatchesDevice` поверне true. Це прямо випливає з правила A6, тож не провал критерію, а
   залишковий сценарій, який варто назвати вголос.
3. **Дубль списку міст.** `tests/skyTimezone.test.js:15-26` дзеркалить `SKY_CITIES` вручну (бо стор
   тягне Vue+Capacitor). Зараз списки збігаються (звірив програмно, 10/10 ключ+пояс), але жоден тест
   цього не стереже: додане місто без поясу пройде повз усі 11 нових тестів.
4. **`ios/App/App/config 2.xml`** — стрей-файл у робочому дереві, він і дає третій issue у D2.
5. **`.state/dev/memory.md` і `STATUS.md`** у діффі — не оцінював, поза приймальними критеріями.

## Що треба для прийняття

1. Прибрати `ios/App/App/config 2.xml` (або підтвердити, що він потрібен, і закрити правило в ai-ops)
   → повторний `npm run ai:scan:all` має дати **2 issues**, як у зрізі. Це єдиний провалений критерій.
2. Скріншот симулятора з поясом `America/Los_Angeles`, дефолтний Київ, перший запуск, без взаємодії:
   видно `05:25` / `20:42` / `15год 17хв` і мітку `UTC+3` біля міста → закриває B1 і візуальну частину B2.
3. Прогін екранів неба в режимі «в літаку» (або вкладка мережі) з підтвердженням «нових запитів немає»
   → закриває D4.
4. Закомітити, змержити, дати посилання на зелений прогін job «Lint and Test» з двома не-UTC кроками
   → закриває C4.
5. Рішення по календарю Місяця (пункт 1 «поза межами») — виправити або зафіксувати як свідомий вибір.

Після 1–4 — повторний qa лише по цих чотирьох пунктах, розрахункову частину перепрогонювати не треба.
